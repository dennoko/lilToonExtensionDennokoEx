// Intentionally empty. Do not delete this file or its .meta.
//
// Released versions shipped the in-memory mask preview here. It was replaced by
// DennokoExPackedMaskStore / DennokoExPackedMaskWatcher. A .unitypackage import never deletes files,
// so this stub keeps the original GUID and overwrites the old script when users update; otherwise
// the leftover code would fail to compile (it calls removed APIs) and break the whole editor assembly.
