// Intentionally empty. Do not delete this file or its .meta.
//
// Released versions shipped an NDMF build-time mask packer here. Packing is now persistent
// (DennokoExPackedMaskStore) and checked before upload by Editor/VRCSDK/DennokoExPackedMaskBuildHook,
// so NDMF is no longer used. A .unitypackage import never deletes files, so this stub keeps the
// original GUID and overwrites the old plugin, which would otherwise fail to compile.
